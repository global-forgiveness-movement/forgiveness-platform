---
phase: quick-260901-i0z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contact/index.html
  - groups/index.html
  - workbooks/index.html
  - index.html
  - css/site.css
  - js/site.js
  - js/data.js
  - js/auth.js
  - workbook/index.html
  - workbook/css/style.css
autonomous: true
requirements:
  - WYATT-260901-item-1-contact-404
  - WYATT-260901-item-2-groups-anchor
  - WYATT-260901-item-3-chooser-move-reword
  - WYATT-260901-item-4-start-it-now-top
  - WYATT-260901-item-5-workbook-platform-bar
  - WYATT-260901-item-6-my-path-flash
  - WYATT-260901-item-7-navy-band-buttons
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Item 1: the contact page's 'Contact via the program page' button resolves 200 at the /post/global-forgiveness-movement address; a repo grep finds ZERO remaining links to the dead old path (hfh.fas.harvard.edu/global-forgiveness-movement without /post/) in served files"
    - "Item 2: at ~900px viewport, following the group-form anchor lands with the 'Tell us about your group' heading visible near the top — not form fields with the heading cut off, not unrelated content above the form (screenshot proves it)"
    - "Items 3+4: workbooks page reads top to bottom: hero → 'Start it now' (copy inside unchanged) → the two main workbook cards → languages → 'Which workbook is right for me?' (ONE question, Wyatt's locked wording verbatim) → 'If REACH helped…' → navy band; section backgrounds still alternate"
    - "Item 3 copy is verbatim: question 'Would you prefer the standard edition, or one framed within Christian faith?', buttons 'Standard edition' / 'Church edition' jumping to #reach / #church as today's do, muted line 'Seeking to receive God's forgiveness yourself? That's the RECEIVE workbook, just below.' — flagged in SUMMARY as new copy pending Wyatt's veto (rule 11)"
    - "Item 5: /workbook/ shows a slim (~44px) cream platform bar above the app — leaf + 'Global Forgiveness Movement' wordmark linking ../, right-side 'My Path' link to ../my-path/ — quiet, brand fonts/colors, workbook UI and CSP untouched, commented as a deliberate exception, not a shell fork"
    - "Item 6: the true cause of the My Path flash is named in the SUMMARY with early-frame evidence captured BEFORE the fix; after the fix, loading /my-path/ signed-in paints no signed-out flash"
    - "Item 7: every button on a navy band (.section--navy) is solid #00887A with WHITE text — the sage link tint no longer cascades into any .btn (Wyatt mid-flight correction 1 Sep: the greyed look is the green TEXT on top of the teal, not the teal fill); white-on-teal contrast (4.37:1) reported in SUMMARY, hex not altered"
    - "Stamp is 'GFM-V1 · 2026-09-01c'; the batch is committed but NOT pushed — held from main for Wyatt's read (orchestrator pushes)"
  artifacts:
    - "contact/index.html + groups/index.html — /post/ URLs with a breadcrumb comment pointing at the HFH constant"
    - "workbooks/index.html — sections reordered, chooser reworded to one question"
    - "index.html — navy-band 'Start a Forgiveness Group' is btn--primary, inline sage style gone"
    - "css/site.css — .section--navy a:not(.btn) tint scoping; scroll-margin-top on anchored sections; figure.video pre-hydration aspect-ratio (if reproduction confirms video-shift)"
    - "js/auth.js — flash fix at the one auth layer (per diagnosis)"
    - "workbook/index.html + workbook/css/style.css — .platform-bar markup + styles"
    - "js/site.js — STAMP 'GFM-V1 · 2026-09-01c'"
    - "Screenshots in the task directory: workbooks top+bottom (1280+390), groups anchor landing at ~900px, workbook bar (1280+390), both navy bands, item-6 before/after frames"
  key_links:
    - "css/site.css:110 — `.section--navy a { color: var(--sage) }` has specificity (0,1,1) and BEATS `.btn--primary` (0,1,0): this is item 7's root cause; the :not(.btn) exclusion is the one-rule fix that also covers the workbooks navy band"
    - "js/auth.js:14-17 — onAuth(cb) calls cb(user) synchronously while user is still null; my-path (line 118) un-hides the signed-OUT section from that initial null, and mountAuth paints 'Sign in / Create account' — the suspected flash chain to confirm"
    - "contact/groups static hrefs ↔ js/data.js HFH constant comment: the breadcrumb keeps the next migration a one-grep fix"
    - "chooser buttons → #reach / #church card ids rendered by the workbooks-main grid (applyLang path untouched)"
---

<objective>
Wyatt's 1 Sep site-review batch: seven fixes across the contact page, groups page,
workbooks page, the interactive workbook shell, My Path, and navy-band button styling.
All seven are LOCKED rulings — no scope discretion; the only judgment calls are the
diagnosed mechanisms behind items 2 and 6, which must be proven in a browser first.

Purpose: everything here is something Wyatt saw with his own eyes on 1 Sep. A 404
on the contact page, a mis-landing anchor, a chooser that asks two questions where
one will do, the screen option buried mid-page, a workbook with no way back to the
site, a flash on My Path, and buttons that read disabled on navy. Each fix makes the
site more useful to a visitor and more trustworthy at a glance.

Output: seven fixes committed (HELD from main — Wyatt reads the batch before the
orchestrator pushes), stamp 2026-09-01c, a SUMMARY that (a) names the true item-6
cause with frame evidence, (b) flags the item-3 wording and item-4 retitle as new
copy pending Wyatt's veto, (c) reports the 4.37:1 white-on-teal contrast, and
(d) cites which ruling each fix used (rule 10).

Size (rule 3, for the SUMMARY's plain-English recap): a visitor today hits a Harvard
404 from our contact page, lands confusingly when jumping to the group form on a
tablet, and meets a "greyed-out" call-to-action on every navy band; after this, all
seven review items are closed. Covers the whole 1 Sep list. Leaves undone: nothing
from the list; the contact-inbox TODO (which email receives mail) stays open with Kate.
</objective>

<coverage_audit>
| Source item | Covered by |
|---|---|
| Item 1 — contact 404 / old HFP URL | Task 1 |
| Item 2 — groups anchor at ~900px | Task 1 (fix) + Task 3 (verify) |
| Item 3 — chooser move + one-question reword | Task 1 |
| Item 4 — "Start it now" to top | Task 1 |
| Item 5 — workbook slim platform bar | Task 2 |
| Item 6 — My Path flash: diagnose then fix | Task 2 |
| Item 7 — navy-band buttons solid teal/white | Task 1 (fix) + Task 3 (contrast report) |
| Cross-cutting — stamp bump, held push, copy flags | Tasks 1, 3 + SUMMARY |

All seven items covered; nothing deferred, nothing simplified.
</coverage_audit>

<context>
Container facts (from STATE.md + orchestrator, verified patterns):
- Serve locally: `python3 -m http.server` from the repo root (headless Chromium
  cannot reach github.io through the egress proxy; byte-identity makes local equivalent).
- Playwright at /tmp/claude-0/-home-user/142384f0-2593-5d9f-97de-b6ce63376dfa/scratchpad/node_modules/playwright,
  `executablePath: '/opt/pw-browsers/chromium'`, args `['--no-sandbox']`, no proxy
  for localhost (`proxy: {server: process.env.HTTPS_PROXY, bypass: 'localhost,127.0.0.1'}`
  only if the page must fetch through the proxy; plain launch works for localhost).
- Gate password `reach1`; or pre-seed `localStorage['gfm.gate.v2'] = 'open'` before load.
- Firebase SDK (www.gstatic.com) is UNREACHABLE from this container's browser —
  content/auth fall back to defaults in 2.5s; wait ≥2.5s before any screenshot.
- Rule 16: kill the server and every browser before replying.
</context>

<tasks>

## Task 1 — Site-page batch: items 1, 2, 3, 4, 7 + stamp (rules 8, 10, 11)

**Files:** contact/index.html, groups/index.html, workbooks/index.html, index.html,
css/site.css, js/site.js, js/data.js

### Item 1 — dead HFP URL (ruling: converge on the one constant, or update to /post/)

Diagnosis already made: the repo-wide grep for `hfh.fas.harvard.edu` finds exactly TWO
served-file stragglers on the dead old path (404s with NO redirect):
- `contact/index.html:22` — the "Contact via the program page" button
- `groups/index.html:68` — the Community of Practice "Register" button

All other hfh links are fine (/post/… in js/data.js, /team/… on about, bare-root links).

Fix: update both hrefs to `https://hfh.fas.harvard.edu/post/global-forgiveness-movement`.
These are static anchors in pages with no render path for them — hardcoding the working
URL is the ruling's sanctioned second option; wiring a JS href-rewrite just for two
links would add a moving part for nothing. Make the next migration a one-grep fix:
extend the HFH constant's comment in js/data.js (top, ~line 6-10) to note that
contact/index.html and groups/index.html carry this URL verbatim as static anchors —
grep `hfh.fas.harvard.edu` on any future move. Cite ruling item-1 in the SUMMARY.

### Item 2 — group-form anchor lands wrong at ~900px (ruling: fix the anchor targeting; verify at ~900px)

Reproduce FIRST (one browser run, before editing): cold-load
`http://localhost:PORT/groups/#groupForm` at 900×700 and screenshot the landing;
also load `/groups/`, click "I want to lead a group" (#lead), screenshot. Known
mechanics to check against (name which one the frames show):
- The `.site-head` is NOT sticky (no position rule in css/site.css) — a sticky-header
  overlap is NOT the mechanism, whatever the ruling's parenthetical guessed.
- Content ABOVE the anchor hydrates late and grows the page after the browser's
  anchor scroll: the shell header is prepended by site.js `buildShell()` after first
  layout; `figure.video` slots are EMPTY (≈0px) until `hydrateVideos()` injects a
  16:9 iframe (the groups hero video and both series-card videos sit above the form);
  the events list renders async with the 2.5s store fallback. Each shift leaves the
  viewport too high relative to the form — matching Wyatt's words.

Fix (apply the ruled parts; add the shift fixes the reproduction confirms):
1. Anchor on the section, not the bare form: add `id="tell-us"` to the
   `<section class="section">` wrapping "Tell us about your group" (groups line 75);
   change contact/index.html:24's link to `../groups/#tell-us`. Do NOT touch the
   form's `id="groupForm"` — groups' inline script reads it (line 125).
2. css/site.css: give anchored sections breathing room —
   `.section[id] { scroll-margin-top: 1rem; }` near the layout rules.
3. If (as expected) frames show the late-hydration shift: reserve video space
   site-wide with ONE rule — `figure.video:empty { aspect-ratio: 16 / 9; }` — so
   iframe injection no longer moves the page (fixes every video slot on every page,
   rule 8). If a residual miss remains (header prepend + events list), add ONE
   shell-level correction in js/site.js `buildShell()` after `data-shell-ready`:
   if `location.hash` names an element, re-scroll it into view once the initial
   hydration settles (e.g. on window `load` + a rAF). One path, every page's anchors.

Record in the SUMMARY which mechanism the frames actually showed and which of the
three fixes shipped.

### Items 3 + 4 — workbooks page reorder + one-question chooser (locked wording)

Reorder `<main>` in workbooks/index.html to:
1. Hero (`section--paper`, unchanged)
2. "Rather do it on a screen?" section MOVED here, `<h2>` retitled **"Start it now"**
   — every word inside the card unchanged (item 4). Drop its `section--paper` class.
3. workbooks-main grid section (`section--paper`, unchanged)
4. Languages section (unchanged, plain)
5. Chooser section MOVED here (directly above "If REACH helped"), ADD `section--paper`,
   and reword to ONE question — this exact markup shape (copy verbatim, layout may
   flex to one card):
   - `<h2>` stays: `Which workbook is right for me?`
   - Delete the "Two questions, and we'll point you…" muted line (no longer true).
   - ONE `card card--tint` (max-width ~640px) containing:
     - `<h3 style="font-size:1rem">Would you prefer the standard edition, or one framed within Christian faith?</h3>`
     - dl-row with `<a class="btn btn--outline" href="#reach">Standard edition</a>`
       and `<a class="btn btn--outline" href="#church">Church edition</a>`
       (same anchor-jump behavior as today's chooser buttons — no new JS)
     - `<p class="small muted" style="margin:0.8rem 0 0">Seeking to receive God’s forgiveness yourself? That’s the RECEIVE workbook, just below.</p>`
6. "If REACH helped, here is where to go next" (unchanged, plain)
7. Navy band (unchanged position)

Result alternates paper/plain/paper/plain/paper/plain/navy. The inline module
(applyLang, renders, listeners) is order-independent — leave it alone. SUMMARY must
flag the chooser question/buttons/muted line AND the "Start it now" title as NEW COPY
shipping pending Wyatt's veto (rule 11).

### Item 7 — navy-band buttons (ruling + Wyatt's 1 Sep mid-flight correction)

Root cause (confirmed by reading, restate in SUMMARY): the greyed look is the GREEN
TEXT on the teal, exactly as Wyatt said — `css/site.css:110`
`.section--navy a { color: var(--sage); }` is specificity (0,1,1), which beats
`.btn--primary`'s (0,1,0), so the workbooks navy band paints its primary button's
label sage instead of white. The homepage band compounds it with a hardcoded sage
outline: `index.html:94` `class="btn btn--outline" style="border-color:var(--sage);color:var(--sage)"`.

Fix at the source, once:
1. css/site.css:110 → `.section--navy a:not(.btn) { color: var(--sage); }` — the
   navy link tint never touches a button again, on any current or future navy band.
2. index.html:94 → `<a class="btn btn--primary" href="groups/">Start a Forgiveness Group</a>`
   (inline style deleted).
3. Sweep (rule: same gesture, same behavior): the repo has exactly two `.section--navy`
   bands — index.html:88 and workbooks/index.html:78 (the footer is navy but has no
   .btn). Confirm with a fresh grep after the workbooks reorder; verify both bands'
   buttons render solid #00887A / white in Task 3.
4. SUMMARY reports white-on-#00887A contrast = 4.37:1 (just under the 4.5:1 AA small-
   text line; .btn text is ~15.7px semibold). Report only — the hex is Wyatt's ruling.

### Stamp

js/site.js:11 → `export const STAMP = 'GFM-V1 · 2026-09-01c';`

**Verify:** `node --check` passes on js/site.js and js/data.js;
`grep -rn "hfh.fas.harvard.edu/global-forgiveness-movement" --include="*.html" --include="*.js" .`
(from repo root, excluding .planning/) returns no served-file hits. Rendered checks land in Task 3.

## Task 2 — Platform layer: item 5 (workbook bar) and item 6 (My Path flash, diagnose FIRST)

**Files:** workbook/index.html, workbook/css/style.css, js/auth.js (+ my-path/index.html
only if the diagnosis demands it)

### Item 6 — diagnose the flash, then fix the named cause (ruling: no fix without naming the cause)

Reproduce BEFORE touching code, and save the evidence frames:
1. Serve the repo; launch headless Chromium; pre-seed the gate
   (`localStorage['gfm.gate.v2']='open'`).
2. Create an account via /join/ (live Firebase is unreachable here → auth falls back;
   if live-mode sign-up won't complete in-container, reproduce in demo mode by
   temporarily pointing the browser at a profile with a demo session — the flash
   mechanism below fires in BOTH modes because it precedes any backend answer).
3. Navigate to /my-path/ signed-in and capture the first frames (CDP
   `Page.startScreencast`, or a burst of `page.screenshot()` calls starting at
   `domcontentloaded`). Read the frames: what exactly paints first?

Suspected chain (from code reading — CONFIRM against the frames, do not assume):
`js/auth.js:14-17` — `onAuth(cb)` invokes `cb(user)` synchronously while `user` is
still `null` (the backend hasn't answered). On my-path (line 118-120) that initial
null UN-HIDES the signed-out section ("Sign in to see your path") and `mountAuth`
paints "Sign in / Create account" in the header; when `announce()` later fires with
the real user, both swap — the flash. Note: `body{visibility:hidden}` until
`data-shell-ready` (css/site.css:47-49) already prevents an unstyled/gate flash, so
"gate overlay or unstyled body" is probably NOT the cause — say so if the frames agree.

Fix at the ONE auth layer (js/auth.js), not per-page (rule 8):
- Make the initial state tri-state: do NOT broadcast a signed-out answer before the
  backend's first real answer. Defer each listener's initial callback until the first
  `announce()`; keep every later semantic identical.
- Bound the wait: if `impl.init()` rejects OR no announce lands within ~2.5s (the
  store-patience precedent from quick task #8), announce `null` — a signed-out
  visitor, and this container's browser (gstatic unreachable), must still resolve
  to the signed-out view rather than a permanently blank page.
- Sweep every consumer and confirm each tolerates a deferred initial callback:
  grep `onAuth` — js/auth.js mountAuth (header slot keeps its static
  "Sign in / Create account" until the first real answer), my-path/index.html:118
  (both `data-when` sections start hidden → calm blank paper until known — the
  desired no-flash state), workbook/bridge.js:27, and any join/ usage.
- The header slot's own signed-in swap ("Sign in" → "My Path · Wyatt") is the same
  class of flash: improve it ONLY if the fix is cheap and does not delay the
  signed-out majority (executor's call; state the choice in SUMMARY).
- SUMMARY: the named cause, the before/after frames, and demo-mode + signed-out
  behavior re-verified.

### Item 5 — slim platform bar on the interactive workbook (locked ruling)

workbook/index.html — insert BEFORE `<div id="app">`:

- `<header class="platform-bar">` containing: an `<a href="../">` with the inline
  leaf SVG (same path as the favicon: `M12 2C7 7 4 12 4 16a8 8 0 0 0 16 0c0-4-3-9-8-14z`,
  fill `#00887a`, `rotate(40 12 12)`, `aria-hidden="true"`) plus the wordmark text
  "Global Forgiveness Movement"; and a right-side `<a href="../my-path/">My Path</a>`.
- An HTML comment stating: this bar is a deliberate small exception to the one-shell
  rule — the workbook is a self-contained app with a stricter CSP and its own design;
  the bar is two static links, NOT a fork of the site shell (changes to the shell do
  not need mirroring here).

workbook/css/style.css — a few lines, using the brand tokens the file already carries:
- `.platform-bar`: flex row, space-between, align-items center, `min-height: 44px`,
  `padding: 0 var(--pad)`, `background: var(--cream)`, `border-bottom: 1px solid var(--line)`,
  `font-family: var(--font-head)`, font-size ~0.85rem; brand link navy (`var(--navy)`),
  no underline, flex/gap for leaf (~20px) + text; "My Path" link `var(--teal-deep)`.
- Keep dark screens honest: `.page { min-height: 100vh }` (line 66) now overshoots by
  the bar's height — introduce `--platform-bar-h: 44px` and set
  `.page { min-height: calc(100vh - var(--platform-bar-h, 0px)) }` so navy screens
  don't gain a needless scrollbar.
- Quiet by design: no shadow, no bold weight, nothing animated. It must not compete
  with the workbook's own `.top` bar below it.

CSP needs NO change (static same-origin HTML/CSS). The shared gate (`position:fixed;
inset:0`) still covers the whole viewport including the bar — correct, leave it.

**Verify:** `node --check js/auth.js`; item-6 before-frames captured and read BEFORE
the auth edit, after-frames captured after; workbook bar renders at 1280 and 390 wide
with the workbook UI intact below it (rendered proof lands in Task 3's full pass).

## Task 3 — Rendered QA pass (rules 4, 6, 16) and the held handoff

Serve the repo root; drive headless Chromium (config in <context>). Wait ≥2.5s after
each load for the store fallback before any capture. Capture, then READ, every shot:

1. /workbooks/ full-page top AND bottom at 1280 and 390 — order is hero → Start it
   now → main cards → languages → one-question chooser (locked copy verbatim) →
   If REACH helped → navy band; chooser buttons jump to #reach/#church; backgrounds
   alternate; navy band button solid teal with white text.
2. Cold-load /groups/#tell-us (and the contact link path) at 900×700 — the "Tell us
   about your group" heading is visible near the viewport top after all hydration
   settles (this is item 2's acceptance shot). Also click "I want to lead a group"
   and confirm #lead lands sanely.
3. / (homepage) navy band "Begin today" — both buttons; "Start a Forgiveness Group"
   is solid #00887A with white text; read the pixels, not the class list.
4. /workbook/ at 1280 and 390 — slim cream bar above the workbook, wordmark left,
   My Path right, workbook UI unchanged below; click-through: bar brand → site home,
   My Path → /my-path/.
5. /contact/ — the program-page button href is the /post/ URL; curl the URL through
   the proxy for a 200 if reachable, otherwise verify against js/data.js's constant
   and say which check ran.
6. Item-6 evidence pack: the before-frames (from Task 2) beside after-frames of
   /my-path/ signed-in showing no signed-out paint.
7. Compute and record white-on-#00887A contrast (expect 4.37:1) for the SUMMARY.

Then: commit code + planning docs, and STOP — do not push. The batch is HELD from
main until Wyatt reads it (orchestrator handles the push). Kill the http.server and
every Chromium process before replying (rule 16).

SUMMARY must carry: per-item ruling citations (rule 10); the item-3/item-4 new-copy
veto flags (rule 11); the item-2 and item-6 true diagnoses with evidence; the
contrast report; the screenshot inventory with one line of what each shows.

</tasks>

<verification>
- All seven items observable in the screenshots, per the truths above.
- Zero served-file hits for the dead old HFP path.
- `node --check` clean on every touched .js file.
- Stamp reads 2026-09-01c in js/site.js.
- No stray server/browser processes after the final reply.
</verification>

<success_criteria>
Seven of seven review items closed with rendered proof; two diagnoses (items 2, 6)
named from evidence, not assumption; new copy flagged for veto; batch committed and
held; SUMMARY written to this directory as 260901-i0z-SUMMARY.md.
</success_criteria>

<commits>
1. fix(review-1sep): contact/CoP URLs to /post/; group-form anchor targeting; workbooks reorder + one-question chooser; navy-band buttons solid teal/white; stamp 2026-09-01c
2. feat(workbook): slim platform bar; fix(auth): My Path flash — [named cause] (split into two commits if cleaner: bar, then auth)
3. docs(quick-260901-i0z): plan, summary, state
— committed, NOT pushed: held for Wyatt's read.
</commits>
